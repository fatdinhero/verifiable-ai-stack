# CrewAI Agents – Auto-generated from masterplan.yaml
{% for mod in masterplan.modules if mod.status == "accepted" %}
{{ mod.id }}:
  role: "{{ mod.name }} Specialist"
  goal: "Operate within {{ mod.layer }} layer."
  backstory: "{{ mod.description }}"
{% endfor %}
