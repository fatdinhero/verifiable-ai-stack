[project]
name = "cognitum-governance"
version = "1.2.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0","pyyaml>=6.0","jinja2>=3.1","yamale>=5.0","networkx>=3.0"]
[project.optional-dependencies]
dev = ["pytest>=8.0"]
